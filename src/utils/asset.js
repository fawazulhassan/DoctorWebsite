/**
 * Returns the public path for healthcare-home assets.
 * @param {string} filename - Asset filename (e.g. 'hero-img-1.png')
 * @returns {string} Full path (e.g. '/healthcare-home/hero-img-1.png')
 */
export const ASSET = (filename) => `/healthcare-home/${filename}`;
